# Propaid 작업 지침

이 저장소에서 작업을 시작할 때는 사용자가 제품 방향을 다시 설명하도록 요구하지 말고 아래 순서로 현재 맥락을 복구한다.

1. `docs/PRODUCT_DIRECTION.md`에서 장기 제품 방향과 우선순위를 읽는다.
2. `docs/PROJECT_STATUS.md`에서 현재 구현 단계와 바로 다음 작업을 읽는다.
3. `README.md`, `git status --short`, 최근 커밋, 관련 코드를 확인해 문서와 실제 구현이 일치하는지 검증한다.
4. 사용자의 새 요청이 없으면 `PROJECT_STATUS.md`의 `다음 작업` 맨 위 항목을 후보로 제안한다. 사용자가 구현을 요청한 경우에만 구현을 시작한다.

## 제품 원칙

- 최우선 제품은 한국 크리에이터·프리랜서·1인 사업자를 위한 AI 협찬·외주 메일 정리 및 거래 관리 서비스다.
- Propaid의 기존 구독·비용 관리는 폐기하지 않고 `제안 → 계약 → 작업 → 입금 → 비용 → 실제 수익` 흐름의 비용 영역으로 연결한다.
- MVP에서는 Gmail 전체 읽기 권한을 요구하지 않는다. 초기 입력은 직접 붙여넣기, 다음 단계는 사용자별 전달 주소와 Resend Inbound 방식이다.
- AI 결과는 자동 확정하지 않는다. 사용자가 원문과 구조화 결과를 확인하고 저장하는 흐름을 유지한다.
- 명시되지 않은 계약 조건은 추측하지 않고 누락 위험 또는 확인 필요 항목으로 표시한다.
- 외부 연동은 핵심 도메인과 분리하고, API 키 없이도 로컬 빌드와 핵심 기능이 동작하게 유지한다.

## 상태 문서 갱신 규칙

기능 구현이나 중요한 의사결정이 끝나면 같은 작업에서 `docs/PROJECT_STATUS.md`도 갱신한다.

- 완료된 기능을 `현재 구현 상태`에 반영한다.
- `다음 작업`에서 완료 항목을 제거하거나 다음 순서로 옮긴다.
- 새 결정과 보류 사유가 있으면 `결정 사항`에 짧게 기록한다.
- 추측으로 완료 처리하지 말고 코드와 테스트로 확인한 사실만 기록한다.
- 긴 배경 설명은 `PRODUCT_DIRECTION.md`에, 자주 변하는 진행 상황만 `PROJECT_STATUS.md`에 둔다.
